name: flutter_test_project
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.3                       # 更新到最新稳定版本
  dio: ^6.1.0                         # Dio 网络请求
  font_awesome_flutter: ^10.2.1
  cupertino_icons: ^1.0.8
  json_annotation: ^4.9.0             # 用于 JSON 序列化

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.3.3                # 用于生成 JSON 序列化代码
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
  assets:
    - lib/home_page/container/
    - lib/splash_screen_page/
    - lib/splash_screen_page/logo.png
    - assets/images/行情页广告图.png
    - assets/images/行情页指数图.png
    - assets/images/news_red.png
    - assets/images/news_blue.png
